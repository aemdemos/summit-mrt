/**
 * Workspace-relative root for navigation validation artifacts (JSON, scripts copy, debug.log).
 * Must match `VALIDATION_DIR` in `hooks/nav-validation-gates/helpers.js`.
 */
export const VALIDATION_DIR = 'migration-work/navigation-validation';
